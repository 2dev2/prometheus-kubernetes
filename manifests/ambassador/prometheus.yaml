apiVersion: monitoring.coreos.com/v1
kind: Prometheus
metadata:
  name: prometheus-ambassador
  labels:
    prometheus: frontend
spec:
  serviceMonitors:
    matchLabels:
      tier: frontend
      service: ambassador-monitor
  resources:
    requests:
      memory: 200Mi